[project]
name = "genesis-lab"
version = "0.1.0"
description = "A research instrument for self-organising systems on Apple Silicon"
requires-python = ">=3.11"
dependencies = ["mlx>=0.30", "numpy>=2.0"]

[tool.setuptools.packages.find]
include = ["genesis*"]

